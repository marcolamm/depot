<% if notice %>
<p id="notice" ><%= notice %></p>
<% end %>

<h1>Store</h1>
<!-- <p>Find me in app/views/store/index.html.erb</p>
 -->
<% @products.each do |product| %>
  <div class="entry">
    <%= image_tag(product.image_url) %>
    <h3><%= product.title %></h3>
	
		description: <%=sanitize product.description %>
 <div class="price_line" >
price: CHF <span class="price" ><%= product.price %></span>
</div>
  </div>
<% end %>
